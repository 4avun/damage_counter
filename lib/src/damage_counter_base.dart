import 'package:damage_counter/src/damage_counter_action.dart';
import 'package:damage_counter/src/damage_counter_node.dart';

class DamageCounter {
  void update(List<DamageCounterAction> actions) {
    for (var action in actions) {
      
    }
  }
}